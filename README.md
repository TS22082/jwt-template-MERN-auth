# jwt-template-MERN-auth
A template to use for future projects. 

## Setup
- create a `.env` file in project root folder
```
JWT_SECRET=<anysecrethere>
```

## To run
to start backend server and client run `npm run dev`
